@model Hrc.Models.Commons.Contact

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Contact</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Type)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Value)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Value)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "Contacts", new { id = @ViewBag.ID, contactid = @ViewBag.ContactID, title = @ViewBag.Title, bcontroller = @ViewBag.Controller, baction = @ViewBag.Action }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", (string)@ViewBag.Action, (string)@ViewBag.Controller, new { id = @ViewBag.ID }, null)
        </div>
    }
</div>
